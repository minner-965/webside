# 独立站项目执行基线（长期记忆）

## 1. 项目目标（你反复强调的核心）
- 站点是欧美生活方式杂货店，不走旧赛道，不要成人/敏感方向文案。
- 前台要可买、可支付、可下单、可回看订单。
- 后台要像电商商家后台：紧凑、实用、可发布、可编辑、可管库存、可管订单、可导出。
- 前后端与数据库必须强同步，不能出现“后台改了前台不变”。

## 2. 系统架构总览
- 前端：`/src` 下 React + Vite（`www.sexwomen.mom` 商店页，`admin.sexwomen.mom` 管理页）。
- 后端：`/server.mjs`（Express API，鉴权、商品、库存、订单、支付回调、导出）。
- 服务器：Render 单服务承载 `www` 与 `admin`，通过 Host 路由分发。
- 数据库：Neon Postgres（生产真源），`/api/health` 需返回 `storage: "postgres"`。

## 3. 四大模块职责与同步关系

### 3.1 前端（Storefront）
- 展示商品、分类、详情、购物车、结账。
- 只消费 API，不做“本地假数据覆盖生产”。
- 支付成功后显示独立弹窗（可截图保存订单号），不再占首页长条。

### 3.2 后端（Admin + API）
- 提供商品发布/下架、库存调整、订单状态、首页文案、大图位配置。
- 订单落库与库存扣减走统一链路（幂等，避免重复写单/重复扣库存）。
- 导出接口统一支持 `csv` 与 `xlsx`。

### 3.3 服务器（Render）
- 环境变量统一在 Render 管理，不写死仓库。
- 发布后先查服务日志，再查 `/api/health`、`/api/version`（如启用）。
- 自定义域名：`www.sexwomen.mom`（前台）、`admin.sexwomen.mom`（后台）。

### 3.4 数据库（Neon Postgres）
- 商品、订单、库存流水、首页内容是数据库单一真源。
- 关键表：`products`、`order_items`、`orders`、`inventory_ledger`、`homepage_content`。
- 订单金额拆分字段：`subtotal/shipping/tax/discount/total/currency` 等必须完整。

## 4. 关键业务闭环（必须长期成立）

### 4.1 商品发布闭环
- 后台 `Publish/Unpublish` -> 写库 -> 前台刷新可见性立即一致。
- 下架后前台消失；上架后前台恢复。

### 4.2 删除闭环
- 删除是软删（回收站），支持恢复与彻底删除。
- 前台默认过滤软删商品。

### 4.3 库存闭环
- 后台 `+/-` 先改草稿，再点“确认”提交。
- 支付成功后才自动扣库存（按你确认策略）。
- 所有库存变更写入 `inventory_ledger`，可追溯。

### 4.4 订单闭环
- Stripe 成功支付后，必须落 `orders + order_items`。
- 同步写库存流水并更新前台库存展示。
- 后台订单状态采用“选择 + 确认”提交，不做即改即写。

### 4.5 支付金额一致性
- 站内账单与 Stripe 左侧账单保持一致：`Subtotal + Shipping + Tax - Discount = Total`。
- 包邮开关先灰色禁用（coming soon），字段先打通，后续可启用。

## 5. Admin 使用与设计原则
- 页面紧凑，信息密度优先，不要大而空。
- 模块分区清晰：概览、商品发布、商品编辑、库存管理、订单管理、首页文案、系统维护。
- 去掉无用说明、技术腔、AI执行提示文案。
- 右上角保留 EN/CN 切换（后台界面文案可切换）。

## 6. 导出规范
- 订单导出：CSV + XLSX。
- 利润/趋势导出：CSV + XLSX。
- 库存与流水导出：CSV + XLSX。
- 导出按钮放模块头部，命名统一。

## 7. 发布与验收 SOP（每次照做）

### 7.1 发布前
- `npm run lint`
- `npm run build`
- 检查关键环境变量（数据库、支付、邮件、管理员账户）。

### 7.2 发布后 5 分钟验收
1. 打开前台可浏览与加购。
2. 后台上架/下架一个商品，前台刷新同步。
3. 支付一笔测试单，后台订单可见。
4. Neon 检查 `orders/order_items/inventory_ledger` 有新增。
5. `/api/health` 返回 `{"ok":true,"storage":"postgres"}`。

## 8. 常见问题排查优先级
1. 先看 Render 日志（启动错误、端口、数据库连接）。
2. 再查 `/api/health` 与支付回调日志。
3. 再查 Neon 数据是否真实写入。
4. 最后查前端缓存/样式覆盖问题。

## 9. 多 Agent 协作硬规则（必须执行）
- 按需开启子 agent，不为“看起来高级”而开启。
- 子 agent 一旦开启不得闲置，必须有明确任务与交付。
- 每个子 agent 必须有文件边界，避免互相覆盖。
- 主 agent 负责总控：分工、集成、回归、发布验收。
- 任何回传结果都必须二次核验后再合并。

## 10. 当前项目状态（本文件更新时）
- 前台：可浏览、可加购、可结账，支付成功后独立弹窗确认。
- 后台：支持发布/下架、软删回收站、库存草稿确认、订单状态确认、导出 CSV/XLSX。
- 数据库：Neon 已接入并作为生产真源。
- 仍需持续优化：后台视觉进一步精炼、移动端细节与性能分包。
